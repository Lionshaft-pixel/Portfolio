DISCORD_WEBHOOK_URL=

# Optional: persistent per-IP visit counters.
# Use either Vercel KV env vars or Upstash Redis REST env vars.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: override the allowed browser origins for the notification endpoint.
VISITOR_ALLOWED_ORIGINS=https://glitchyn.online,https://project-a7soe.vercel.app

# Optional: comma-separated IP addresses that should not trigger Discord messages.
VISITOR_IGNORE_IPS=
